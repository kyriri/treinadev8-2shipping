<h1>Ordens de Serviço</h1>

<div class='container'>
  <% @building_blocks.each do |box| %>
    <div class='<%= box[:css_class] %>'> 
      <em><%= box[:title] %></em>
      <% if box[:collection].empty? %>
        Nenhuma ordem de serviço.
      <% else %>
        <ul>
          <% box[:collection].each do |order| %>
            <li>ID: <%= order.id %> | <%= link_to 'detalhes', order %></li>
          <% end %>
        </ul>
      <% end %>
    </div>
  <% end %>
</div>